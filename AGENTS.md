# Agent Instructions

- **Do not test Ollama features.** The environment does not support Ollama, so any tests relying on it will fail. Skip these tests or ensure they are mocked if they must be run.
